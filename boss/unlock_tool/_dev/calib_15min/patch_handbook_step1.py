# -*- coding: utf-8 -*-
"""手册①分支改造：每次分析前必须人工确认拖框，无条件豁免"""
import io

p = 'AI工作手册.md'
t = io.open(p, encoding='utf-8').read()

old = '''① 已有该游戏的 config.json？
   ├─ 有，且不触发复用确认（见①A）→ 跳到 ③
   ├─ 有，但触发复用确认（见①A）→ 先走①A逐类签字，再跳 ③
   └─ 没有 → 先做标定（见第3节），再做 ③

①A 复用配置的逐类确认（2026-08-31 灵画师复盘新增；触发即强制，不得静默跳过）
   触发条件（满足其一）：
   a) config 来自本工具包之外（同事转发/其他盘拷入）；
   b) calib_note 里存在 `[AI改·未确认]` 标记（AI 曾改动分类、人工未签字）。
   豁免：人类明确说"配置没问题直接跑"可跳过，豁免原话记入 calib_note。
   流程：抽干净主页帧，按 礼包面板 → 副本面板 → 养成系统面板 顺序逐类渲染
   （render_boxes/grid_overlay）、逐类询问签字（同⓪A第3步），全部签字后
   才可开始分析。
   背景：灵画师'''

new = '''① 每次分析前必须人工确认拖框（2026-09-02 升级：无条件，每视频每跑必走）
   不管 config.json 是否已存在、不管是否同个游戏、不管之前签过多少次——
   只要用本工具分析视频，就必须先渲染三类框的提案图给人类过目签字，
   人类回一句"没问题"后才能进入分析。没有例外，没有豁免。
   ├─ config.json 已存在 → 用已有 rois 渲染提案图（render_boxes.py），
   │   按礼包面板 → 副本面板 → 养成系统面板 顺序逐类询问签字；
   ├─ config.json 不存在 → 先做标定（见第3节），渲染提案图逐类签字；
   └─ 全部签字后 → 写入 calib_note（记录本日签字），再做 ③。
   背景与教训：跳过这一步会导致 AI 凭已有 config 静默跑分析，
   而标定的框位/语义可能与当前视频实际不符（图标可能已移动/新增/消失），
   人类过目是最后兜底——跳过=放弃兜底，不可接受。

①A 复用配置的逐类确认（与①合并，不再单独设触发条件）
   原按触发条件跳过的设计（2026-08-31 版）已废弃，
   统一走①的"每次必确认"流程——无例外。

确认后 AI 在 calib_note 追加一行：日期 + "人类签字确认" + 视频'''# —— 上一行末尾是后续章节接续点

'''
   流程：抽干净主页帧，按 礼包面板 → 副本面板 → 养成系统面板 顺序逐类渲染
   （render_boxes/grid_overlay）、逐类询问签字（同⓪A第3步），全部签字后
   才可开始分析。
   背景：灵画师'''

# 找到旧①整段替换
s = t.index('① 已有该游戏的 config.json？')
e = t.index('② 抽干净主页帧') if '② 抽干净主页帧' in t else t.index('② ', s)
# ①A 末尾到②之间可能还有"背景：灵画师"尾巴，直接找到②的起点
e2 = t.index('\n② ', s)
t = t[:s] + new + t[e2:]
io.open(p, 'w', encoding='utf-8').write(t)
print('手册①分支已改为"每次必确认"')
